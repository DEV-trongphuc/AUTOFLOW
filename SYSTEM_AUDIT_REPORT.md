# BÁO CÁO KIỂM TRA HỆ THỐNG (SYSTEM AUDIT REPORT)

**Thời gian kiểm tra:** 2026-01-15
**Đối tượng kiểm tra:** Cấu trúc Database & Logic Backend (PHP)

## 1. TỔNG QUAN DATABASE
Hệ thống có khoảng **45 bảng**. Cấu trúc nhìn chung khá đầy đủ cho một hệ thống Marketing Automation + AI Chatbot. Tuy nhiên, tồn tại một số vấn đề về nhất quán dữ liệu và tối ưu hóa.

### 🔴 Vấn đề Nghiêm trọng (Critical Issues)
1.  **Trùng lặp cấu trúc (Table Store Redundancy):**
    *   Tồn tại song song các cặp bảng:
        *   `ai_conversations` và `ai_org_conversations`
        *   `ai_messages` và `ai_org_messages`
    *   **Rủi ro:** Gây nhầm lẫn trong logic code. Nếu một file PHP ghi vào bảng này nhưng file hiển thị lại đọc bảng kia, dữ liệu sẽ bị "bay màu". Cần xác định bảng nào là chuẩn (khả năng bảng *_org_* là bảng gốc hoặc bảng tổ chức cũ).

2.  **Không nhất quán kiểu dữ liệu (Inconsistent Data Types):**
    *   **`property_id`**:
        *   `varchar(50)`: trong `ai_chatbot_settings`, `ai_conversations`, `ai_messages`.
        *   `char(36)` (UUID): trong `web_properties`, `web_visitors`, `web_events`.
    *   **Rủi ro:** Khi JOIN giữa các bảng Tracking (Web) và AI (Chatbot), MySQL sẽ **không thể sử dụng Index** hiệu quả do khác kiểu dữ liệu (implicit casting), dẫn đến query rất chậm khi dữ liệu lớn.

### ⚠️ Vấn đề Cần cải thiện (Improvements)
1.  **Khai báo Index:**
    *   Các bảng log lớn như `web_events`, `web_page_views`, `ai_messages` cần composite index (ví dụ: `(property_id, created_at)`) để phục vụ report nhanh. Hiện tại chỉ thấy index đơn lẻ.
2.  **Lưu trữ Vector:**
    *   Bảng `ai_training_chunks` lưu cả `embedding` (Text JSON) và `embedding_binary` (Blob). Nên ưu tiên dùng Binary hoàn toàn để tiết kiệm 70% dung lượng storage và tăng tốc độ tính toán.

## 2. CHECK LOGIC BACKEND (CODE REVIEW)

### ✅ Điểm tốt (Pros)
*   **Worker Flow (`api/worker_flow.php`):**
    *   Sử dụng Transaction an toàn: Tách biệt transaction fetch job và process job.
    *   Concurrency Control: Code xử lý tốt việc nhiều worker chạy song song (Locking row).
    *   Tính năng Priority & Self-healing được implement tốt.
*   **AI RAG (`api/chat_rag.php`):**
    *   Logic tìm kiếm hiện đại: Hybrid Search (Kết hợp Vector Search + BM25 Keyword).
    *   Có cache vector và cache kết quả tìm kiếm.

### 🔴 Điểm cần chú ý (Cons)
*   **Logic PHP `unpack` trong RAG:** Việc unpack binary float (`unpack('f*', ...)`) có thể phụ thuộc vào kiến trúc server (Little Endian vs Big Endian). Cần đảm bảo server Python (nếu có dùng để tạo vector) và server PHP đồng bộ encoding.
*   **Hardcoded Credentials:** File `check_db_structure.php` (và có thể các file cũ khác) đang hardcode user/pass DB. Nên chuyển hết về import `db_connect.php`.

## 3. KHUYẾN NGHỊ HÀNH ĐỘNG (ACTION PLAN)

1.  **Chuẩn hóa ID:** Convert tất cả cột `property_id` sang `char(36)` nếu dùng UUID, hoặc `varchar(50)` thống nhất. Khuyến nghị dùng `char(36)` cho UUID.
3.  **Tối ưu Index:** Chạy script analyze query để thêm Composite Index cho các bảng report.
4.  **Bảo mật:** Quét toàn bộ folder `api/` để tìm các file chứa credential hardcode và replace bằng `require 'config.php'`.

---
*Generated by Antigravity AI Agent*
